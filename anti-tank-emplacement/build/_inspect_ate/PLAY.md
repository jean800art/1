# E/AT-12 血量与载弹量

本 addon 只修改 E/AT-12 反坦克炮台：最大生命值 300 → 3000，弹匣容量 30 → 300。射速和发射模式保持原值。

运行时会核对游戏可执行文件 SHA-256、组件表类型、目标资源哈希、唯一索引和原始字段值。核对失败时拒绝写入。写入前记录完整原记录，写入后回读；禁用 addon 后重启游戏即可恢复原值。

此包尚未在当前游戏进程中验证。仅在私人/离线环境使用；联机时本地修改可能不与队友同步，也可能触发反作弊。
